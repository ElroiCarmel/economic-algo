* If you simply want to get the allocation matrix use the `egalitarian` function.

* If you want to see the solution as text use the `allocate` function.

* To see the doctest results simplt run the `egalit.py`. 